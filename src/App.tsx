/**
 * Componente principal de la aplicación (LEGACY - NO SE USA)
 * 
 * NOTA: Este archivo ya no se usa. El routing se maneja mediante AppRouter en main.tsx
 * Se mantiene por compatibilidad pero no debería ser importado.
 * 
 * @deprecated Usar AppRouter desde router/AppRouter.tsx en su lugar
 */
// Este archivo está deprecado y no se usa
// El routing se maneja en router/AppRouter.tsx
// Se mantiene solo por compatibilidad
